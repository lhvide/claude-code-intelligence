---
name: Example Feedback Memory
description: Template for recording behavioral corrections — rename this file to match the specific feedback
type: feedback
---

## The Correction
[What the user corrected — e.g., "Don't mock the database in integration tests"]

## Why
[The reason behind the correction — e.g., "Mocked tests passed but production migration failed. Mock/prod divergence masked a broken migration."]

## The Rule
[The generalized rule to follow going forward — e.g., "Integration tests must hit a real database, not mocks. Only unit tests may use mocks."]
